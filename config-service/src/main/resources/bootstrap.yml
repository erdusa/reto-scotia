server:
  port: 8080

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/erdusa/reto-scotia.git
          searchPaths: config-data
          default-label: "main"
  security:
    user:
      name: eudis.duarte
      password: Erdusa*8000